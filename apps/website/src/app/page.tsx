'use client';

import { Button } from '@mykelcodes/ui-components';
import { View } from 'react-native';

export default function Home() {
    return (
        <div>
            <View style={{ height: 100, width: 100, backgroundColor: 'teal' }} />
            Home page
            <Button />
        </div>
    );
}
